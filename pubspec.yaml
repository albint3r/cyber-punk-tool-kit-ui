name: cyber_punk_tool_kit_ui
description: This container have a glassy design with a touch of  cyber-punk
version: 0.0.1
homepage: https://github.com/albint3r/cyber-punk-tool-kit-ui.git

environment:
  sdk: '>=3.0.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  gap: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^2.1.2

flutter:
  assets:
    - assets/
